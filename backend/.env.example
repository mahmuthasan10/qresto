# Production Environment Variables (Railway)
# Copy this to .env and fill in the values

# Database (Railway PostgreSQL - provided automatically)
# connection_limit=20 : 1000 kullanici icin yeterli pool
# pool_timeout=10     : 10 saniye bekleyip ConnectionTimeout firlatir
DATABASE_URL=postgresql://user:password@host:5432/database?connection_limit=20&pool_timeout=10

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=production

# CORS (Frontend URL)
CORS_ORIGIN=https://your-frontend-domain.up.railway.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Cloudinary (Optional - for image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Sentry Error Tracking (Optional — production'da set edin)
SENTRY_DSN=

# Email (Optional - for password reset)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
